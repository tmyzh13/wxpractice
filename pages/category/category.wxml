<!--pages/category/category.wxml-->
<view class="main">
  <view class="category-left">
    <view wx:for="{{category}}" wx:key="index" data-id="{{item.id}}" data-index="{{index}}" bindtap="switchTab" class="cate-list {{curIndex==index?'on':''}}">{{item.name}}
    </view>
  </view>
</view> 